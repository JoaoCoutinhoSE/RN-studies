import React from "react";
import { View, Text, StyleSheet } from "react-native";

export function FluxThree() {
  const styles = StyleSheet.create({
    container: {
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
    },
  });

  return (
    <View>
      <Text>Flux Three</Text>
    </View>
  );
}
